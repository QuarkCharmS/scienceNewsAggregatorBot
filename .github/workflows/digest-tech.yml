name: CS / AI / Tech Digest

on:
  schedule:
    - cron: '0 14 * * *'   # 2 PM UTC
  workflow_dispatch:
    inputs:
      count:
        description: 'Number of articles to pick (overrides default of 4)'
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: bot-state
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run CS/AI/tech digest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
          NASA_API_KEY: ${{ secrets.NASA_API_KEY }}
        run: |
          COUNT="${{ github.event.inputs.count }}"
          if [ -n "$COUNT" ] && [ "$COUNT" != "0" ]; then
            python bot.py --tech --count "$COUNT"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            python bot.py --tech --count 2
          else
            python bot.py --tech --count 4
          fi

      - name: Commit state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f .seen_articles.json .last_run_tech 2>/dev/null || true
          git diff --staged --quiet || git commit -m "chore: update tech digest state [skip ci]"
          git push
